[project]
name = "pos-api"
version = "1.0.0"
description = "FastAPI backend mini-service for the Naseeb Biryani POS migration."
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy>=2.0",
    "pydantic>=2",
]

[tool.uv]
package = false

[tool.uv.pip]
# nothing extra needed
